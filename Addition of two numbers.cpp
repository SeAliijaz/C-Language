#include<iostream>
int main()
{
	//Define variable for first , second and sum 
	int firstDigit, secondDigit;
	
	//get input from user
	printf("enter number to add\n");
	scanf("%d%d",&firstDigit,&secondDigit);
	
	printf("sum is %d\n", firstDigit + secondDigit);
	
	return 0;
}
